
function getExp(props) {
    
    return (
        <div>
        <div> <h2>{props.date} </h2></div>        
        <div> {props.title}</div>
        <div> {props.amount}</div>

        </div>
    )
} 

export default getExp;